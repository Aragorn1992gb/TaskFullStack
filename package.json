{
  "name": "TaskFullStack",
  "version": "1.0.0",
  "description": "This is a full stack task",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dockerstart": "docker-compose up",
    "dockerrestart": "docker-compose down && docker-compose build --no-cache && docker-compose up"
  },
  "dependencies": {
    "express": "^4.16.1"
  }
}
